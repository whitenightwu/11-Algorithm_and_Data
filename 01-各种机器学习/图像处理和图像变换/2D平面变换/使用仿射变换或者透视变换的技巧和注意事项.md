# 使用仿射变换或者透视变换的技巧和注意事项
使用仿射变换或者透视变换时，如果需要进行多次变换，则最好合并为一次，否则可能存在像素值的误差。原因就是除了刚体变换（即只有平移和旋转）外，其他所有变换都可能导致像素值的差异。
例如：先缩小再放大，缩小时带来的信息损失是不可逆的。
例如：方案1“从A变换到B，再从B变换到C”与方案2“从A直接变换到C”，两者的虽然肉眼从图像上看是一模一样的，但从数值上看是不一样的。
