# 变点/突变点(change point)
变点/突变点(change point)指的是某一位置或时刻，在此前后观测值或数据遵循两个不同的模型。
异常点是突然的变化，很快就修复了。
异常点和突变点有着极大的相似性，变点再短时间内很容易被认为是异常点。


一般方法：
1）简单的统计值
2）3倍的方差
3）箱型图分析。一般都是1.5倍的IQR（四分差的距离，IQR = Q3 − Q1）
